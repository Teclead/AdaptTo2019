/**
 * This file is used in the build prozess
 * Register your React Apps here
 * to get single builds for each app
 * If you want to put multiple apps in one JS build use string[]
 * If you want to have it in a single build (which is recommended) string
 * key: name of the aem component which uses you react app
 * property: the app / folder name of your app
 *
 *
 */
module.exports = {
  // if multiple use as array
  // myAEMComponent : ["ReactAppOne","ReactAppTwo"],
  cta: 'Cta'
}
